#include <stdio.h>

/**
 * main - prints the required string
 * Return: 0 if successfully exited, or non zero if failed
 */

int main(void)
{
	char str[] = "Programming is like builiding a multilingual puzzle";

	puts(str);
	return (0);
}
